# Grader / inference run artifacts — regenerated by run_eval.py, wash_refs.py,
# grade_stream.py, build_taxonomy.py. Keep the code in graders/ and the sealed
# eval bundle under data/; everything under out/ is per-run scratch.
out/
