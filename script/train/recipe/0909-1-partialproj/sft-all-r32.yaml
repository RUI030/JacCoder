# One-pass SFT over every dataset currently available under each SFT task.
# Embeddings and lm_head stay frozen; standard LoRA is used instead of rsLoRA.
recipe:
  name: sft-all-r32
  stage: sft
  base_model: output/model/JacLLM-CPT-Ornith-9B-v1.2
  adapter: ""
  seed: 3407
  max_seq_length: 4096
  load_in_4bit: true
  text_only: true
  chat_template: null      # preserve the base model's native chat template

hyperparams:
  epochs: 1
  batch_size: 1
  grad_acc: 10
  lr: 2.0e-4
  embed_lr: 0
  optimizer: adamw_8bit
  scheduler: linear
  warmup_steps: 10
  weight_decay: 1.0e-3
  save_steps: 100
  do_eval: false
  lora_rank: 32
  lora_alpha: 16
  lora_dropout: 0
  rslora: false
  bias: none
  grad_checkpt: unsloth
  packing: false
  target_module:
    - gate_proj
    - up_proj
    - q_proj
    - down_proj
    - o_proj
  instruction_part: "<|im_start|>user\n"
  response_part: "<|im_start|>assistant\n"

mixing:
  strategy: concat

datasets:
  - task: code_gen
    name: "*"
  - task: farm
    name: "*"
  - task: js2jac
    name: "*"
  - task: osp
    name: "*"
  - task: py2jac
    name: "*"
  - task: qa
    name: "*"
  - task: scaffold2impl
    name: "*"
