# SFT — rank/2 regression probe against the r=128 concat baseline
# (companion to cpt-r64). Only variable that changes: lora_rank (128 -> 64).
# Mixing stays concat one-epoch-each so any regression is attributable to rank.
recipe:
  name: sft-r64
  stage: sft
  base_model: ornith-ai/Ornith-1.5-9B
  adapter: ""              # set to a CPT adapter path to continue from CPT
  seed: 3407
  max_seq_length: 4096
  load_in_4bit: true
  text_only: true
  chat_template: null      # preserve the base model's native chat template

hyperparams:
  epochs: 1
  batch_size: 1
  grad_acc: 10
  lr: 2.0e-4
  embed_lr: 0
  optimizer: adamw_8bit
  scheduler: linear
  warmup_steps: 10
  weight_decay: 1.0e-3
  save_steps: 100
  eval_steps: 0.1
  do_eval: false
  lora_rank: 64
  lora_alpha: 16
  lora_dropout: 0
  rslora: true
  bias: none
  grad_checkpt: unsloth
  packing: false
  target_module:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
  instruction_part: "<|im_start|>user\n"
  response_part:    "<|im_start|>assistant\n"

mixing:
  strategy: concat

datasets:
  - task: code_completion
    name: Nitin-9k-py2jac-idiom
  - task: code_gen
    name: opus-synth-v2
  - task: farm
    name: Nitin-2k-farm
  - task: js2jac
    name: Nitin-3k-js2jac-idiom
  - task: osp
    name: Nitin-1k-osp
  - task: py2jac
    name: opus-synth-v2
  - task: qa
    name: opus-synth-v2
  - task: scaffold2impl
    name: Rui-jacapp-scaffold
